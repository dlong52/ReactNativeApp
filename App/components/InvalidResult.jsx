import { View, Text } from 'react-native'
import React from 'react'

const InvalidResult = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-[15px] font-semibold">Invalid result</Text>
    </View>
  )
}  

export default InvalidResult